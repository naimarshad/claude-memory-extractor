A `SessionEnd` hook (`~/.claude/hooks/memory_extractor.py`) distills every session's transcript into an Obsidian vault at `~/Obsidian/Claude/claude-memory/` — organized into `Context/`, `Plans/`, `Decisions/`, `Mistakes/`, `DosDonts/`, and `Sessions/`, with a generated `Index.md`. This is a personal cross-session log, not a substitute for repo `CLAUDE.md` files. Check `~/Obsidian/Claude/claude-memory/Index.md` at the start of substantive work in any project and filter to entries whose frontmatter `project:` matches the current repo — it may hold decisions, mistakes, or open plans from earlier sessions that aren't written down anywhere else. If the vault or hook script is missing or looks broken, say so rather than silently skipping it.
